
const {expect, test} = require('@jest/globals');

test('reverse of a', () => {
  expect('a').toBe('a');
});

test('reverse of react', () => {
  expect('tcaer').toBe('tcaer');
});

test('reverse of releveler', () => {
  expect('releveler').toBe('releveler');
});
