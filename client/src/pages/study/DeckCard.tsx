export {}

// import * as React from 'react'

/*
// Third party imports
import { Paper, PaperProps, styled, Typography } from '@mui/material'

import { Deck } from '.'

const CustomizedPaper = styled(Paper)<PaperProps>(({ theme }) => ({
  width: '200px',
  height: '100%',
  '&:hover': {
    backgroundColor: '#b3d1ff'
  }
}))

function DeckCard ({ key, deck }: { key: number, deck: Deck }): JSX.Element {
  return (
    <div style={{ height: '130px', width: '230px', margin: '16px' }}>
      <CustomizedPaper sx={{ boxShadow: 3 }}>
        <Typography variant="h5">{deck.title}</Typography>
        {deck.description}
      </CustomizedPaper>
    </div>
  )
}

export default DeckCard
*/
