export {}

/*
import React from 'react'

// Third party imports
import { Box, BoxProps, styled } from '@mui/material'

// Project imports
import DeckCard from './DeckCard'
import { DeckSet, Deck } from '.'

const CustomizedBox = styled(Box)<BoxProps>(({ theme }) => ({
  display: 'flex',
  overflowX: 'auto',
  width: '100%',
  padding: '0 16px',
  paddingBottom: 7
}))

interface DeckProps {
  name: string
  description: string
  memberFeature: boolean
}

function ListCards ({ category, decks }: DeckSet): JSX.Element {
  // const list: number[] = [1, 2, 3, 4, 5, 6, 7, 8, 9]
  return (
    <>
    <div>{category}</div>
    <CustomizedBox>
      {decks.map((element: Deck) => (
        <DeckCard key={element.id} deck={element}/>
      ))}
    </CustomizedBox>
    </>
  )
}

export default ListCards
*/
